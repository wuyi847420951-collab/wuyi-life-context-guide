---
created: 2026-07-26T22:35
updated: 2026-07-26T23:23
---
# Changelog

本项目版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)：
`MAJOR.MINOR.PATCH`

- MAJOR：结构性的大改动（如文件数量变化、模板字段重排、不兼容旧产出）
- MINOR：新增功能或引导逻辑优化（向下兼容）
- PATCH：文字修订、错别字、注释补充

---

## [1.0.0] — 2026-07-26

### 🎉 首次公开发布

从内部 AI-LIFE-OS 系统抽离并通用化，作为可对外分发的独立 Skill 发布。

### 包含的能力

- **11 个文件的「人生上下文」体系**：内核层（5 份，年更）/ 方向层（2 份，季更）/ 当下层（2 份，周更）/ 传记层（1 份，按需）/ 导航层（1 份，自动合成）
- **5 种使用模式**：首次探索 / 深化 / 校准更新 / 周更快填 / 自动合成
- **三层剪切优先级**：内核 > 方向 > 当下，传记提供背景，不参与优先级覆盖
- **Confidence 成熟度模型**：`0.3 → 0.5 → 0.7 → 0.9` 四档渐进，量化每个维度的深度
- **跨文件关联协议**：后续维度引用前序维度，自动检测矛盾并温和提示
- **会话恢复**：对话中断后可从断点继续
- **完整的产出模板**：frontmatter + 结构化区 + 叙事区 + AI 协作备注三段式

### 兼容性

- 面向能够加载 Markdown Skill、读取文件并写入用户目录的 AI Agent；不同工具的安装与加载方式可能不同
- 产出文件使用标准 Markdown + YAML frontmatter，可在 Obsidian、Logseq、VS Code 等工具中读取
- 不依赖外部 API、数据库或网络服务

### 设计理念

- **一次只聊一个维度**，不强制一次做完
- **问题序列是素材库不是 checklist**——选最有信息增量的 3-5 个就够
- **跟用户的节奏走**——自发聊到的内容优先于引导问题
- **安全边界优先**——用户说跳过就跳过，敏感议题只记录主动表达的部分

---

## 版本号说明（给维护者）

内部迭代期间（公开发布前）的版本号未遵循 SemVer，最高到 v6。
v1.0.0 起重新计数，作为公开分发的基准。
