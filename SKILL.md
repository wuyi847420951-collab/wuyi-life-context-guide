---
name: wuyi-life-context-guide
description: |
  通过 AI 对话帮用户建立、深化和长期维护「人生上下文」；默认生成 11 个核心 Markdown 文件，并可选扩展定位、产品、内容 3 个事业表达文件。支持首次建档、维度深化、校准更新、周更快填、月季校准、自动合成和事业表达梳理。触发词包括：「人生上下文」「建档」「了解我」「价值观梳理」「愿景」「我是谁」「我想要什么」「更新上下文」「深化」「本周状态」「周更」「月度校准」「季度校准」「合成关于我」「个人定位」「梳理产品」「内容方向」。
---

# 🚨 第一步：被加载后立即执行

**只完成定位和恢复，然后停下来等用户回应。**

1. 在当前项目或工作区根目录查找 `.wuyi-life-context-guide.json`。
2. 找到定位文件 → 读取其中的 `base_path`，再读取 `<base_path>/.meta.md`。
3. 未找到定位文件 → 检查默认路径 `上下文系统/我的人生上下文/.meta.md`。

**找到 `.meta.md`** → 读取它并按 `references/00-路由与模式.md` 展示启动菜单，然后停下。

**没有找到 `.meta.md`** → 对用户说：
   - "我来帮你建一份人生上下文——就是 11 个文件，让 AI 真正理解你是谁、在意什么、想往哪走。不是一次做完，以后可以随时补和改。"
   - "你也可以选择开启事业表达层，继续梳理定位、产品和内容；不开启也不影响核心功能。"
   - "文件存在哪里？你有已有的人生上下文文件夹吗？还是使用默认路径 `上下文系统/我的人生上下文/`？"

**停下。等用户回答。**

---

# /wuyi-life-context-guide · 人生上下文引导

> 先理解自己，再把理解转化为方向、行动，以及可选的事业表达。

## 核心架构

```
人生上下文/
├── 关于我.md                 导航（自动合成）
├── 内核/                     年更 — 最稳定的你
│   ├── 精神与愿景.md
│   ├── 价值观与底线.md
│   ├── 能力与天赋.md
│   ├── 行为与节奏.md
│   └── 环境与资源.md
├── 方向/                     季更 — 当前航向
│   ├── 当前渴望.md
│   └── 关键关系.md
├── 当下/                     周更 — 此刻状态
│   ├── 本周状态.md
│   └── 本周焦点.md
└── 传记/                     按需
    └── 我的故事.md
```

**11 个文件。三层剪切优先级：内核 > 方向 > 当下；传记提供背景，不参与优先级覆盖。**

用户主动开启后，增加可选模块：

```
事业表达/
├── 定位.md
├── 产品.md
└── 内容.md
```

关系为：**人生上下文 → 定位 → 产品 → 内容 → 真实反馈 → 校准人生上下文**。

## 默认存储路径

```
{项目/知识库根目录}/上下文系统/我的人生上下文/
```

路径确定后：

1. 在输出目录写入 `.meta.md`；
2. 在当前项目或工作区根目录写入 `.wuyi-life-context-guide.json`：

```json
{"skill_version":"1.1.0","base_path":"用户确认的路径"}
```

定位文件只记录版本和路径，不记录个人内容。后续会话先读定位文件恢复路径。
能使用相对路径时不要记录绝对路径；如果项目受 Git 管理，将该定位文件加入 `.git/info/exclude`，避免公开提交。

## 所有详细逻辑在 references/ 里

根据当前模式和维度，**只加载对应文件**：

| 场景 | 加载 |
|:--|:--|
| 首次激活、回访菜单、路由判断 | `references/00-路由与模式.md` |
| 核心维度 | `references/01-08` 中对应文件 |
| 周更 | `references/周更引导.md` |
| 合成关于我 | `references/09-合成-关于我.md` |
| 事业定位 | `references/10-定位.md` |
| 产品梳理 | `references/11-产品.md` |
| 内容方向 | `references/12-内容.md` |

不要一次性加载全部引用文件。

## 共享协议（所有模式通用）

### 对话风格

- 每次只问一个问题，等待用户回答。
- 回答短时温和追问具体例子；回答长时提取信号，不机械追问。
- 使用用户自己的词，不替用户发明身份、定位或产品价值。
- 每个维度结束时先总结并确认，再写入文件。

### 安全边界

- 这是自我探索工具，不做心理、医疗、法律或财务诊断。
- 涉及创伤、健康、经济和关系时，只记录用户主动表达的部分。
- 用户说“跳过”时立即跳过，不追问原因。
- 不确定的信息标注“待确认”；AI 推导内容明确标注“AI 推断”。

### 数据隐私

- 默认只写入用户确认的本地目录，不主动上传或发送个人内容。
- 如果目标目录位于公开仓库或公开同步区，写入前再次警告并确认。
- 必要时用角色称呼代替第三人的真实姓名。

### 对话灵活性

- 问题序列是素材库，不是 checklist。
- 一次只选择最有信息增量的 3-5 个问题。
- 用户自发谈到的内容优先于预设问题。

### 输出格式

始终使用 `assets/templates/` 中对应文件作为唯一输出模板。`references/` 只提供访谈问题和判断规则，不得自行重建另一套模板。

```yaml
---
layer: [内核/方向/当下/传记/导航/事业表达]
created: YYYY-MM-DD
updated: YYYY-MM-DD
confidence: [—/0.3/0.5/0.7/0.9]
---
```

### .meta.md 状态枚举

`未启用` | `未填` | `进行中` | `种子`(≤0.3) | `基础`(0.5) | `深挖`(0.7) | `成熟`(0.9) | `未合成`

### 更新与版本保护

- 修改任何已有产出前，先按 `references/00-路由与模式.md` 归档旧版本。
- 升级 Skill 时只补字段和缺失模板，不覆盖用户已有正文。
- 展示拟修改摘要或 diff，用户确认后再保存。
- 合成文件必须列明来源文件、缺失维度和 AI 推断。

### 对话结束时的同步

1. 生成或更新维度文件。
2. 同步更新 `.meta.md`。
3. 更新根目录定位文件中的 `skill_version`。
4. 满足重算条件时提示重新合成“关于我”。

---

## 目录结构

```
人生上下文引导/
├── SKILL.md                    ← 入口与共享协议
├── references/
│   ├── 00-路由与模式.md         ← 初始化、迁移、菜单与模式
│   ├── 01-09                   ← 核心维度与合成引导
│   ├── 10-12                   ← 可选事业表达引导
│   └── 周更引导.md
└── assets/
    └── templates/              ← 唯一输出模板真源
```
