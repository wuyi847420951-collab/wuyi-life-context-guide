---
name: wuyi-life-context-guide
description: |
  通过 AI 对话帮用户建立、深化、维护「人生上下文」——11 个文件，让 AI 真正懂你。
  支持首次建档、维度深化、校准更新、周更快填、自动合成五种模式。
  触发词：「人生上下文」「建档」「了解我」「帮我梳理」「价值观梳理」「愿景」「我是谁」「我想要什么」「更新上下文」「深化」「重新聊一下XX」「本周状态」「周更」「本周焦点」「合成关于我」「生成关于我」
---

# 🚨 第一步：被加载后立即执行

**只做以下操作，然后停下来等用户回应。不做其他任何事。**

1. 检查 `上下文系统/我的人生上下文/.meta.md` 是否存在

2. **不存在** → 对用户说以下三句话，然后停下：
   - "我来帮你建一份人生上下文——就是 11 个文件，让 AI 真正理解你是谁、在意什么、想往哪走。不是一次做完，以后可以随时补和改。"
   - "11 个文件不需要一次做完。我们今天先聊一个维度，以后随时可以继续。"
   - "文件存在哪里？你有已经存在的人上下文文件夹吗？还是用默认路径 `上下文系统/我的人生上下文/`？或者你想指定一个新路径？"

3. **存在** → 读 .meta.md，然后停下。对用户说：
   - "你想做什么？" 然后列出启动菜单（见 `references/00-路由与模式.md`）。

**停下。等用户回答。**

---

# /wuyi-life-context-guide · 人生上下文引导

> 通过 AI 对话帮用户建立、深化、维护「人生上下文」——11 个文件，让 AI 真正懂你。

## 核心架构

```
人生上下文/
├── 关于我.md                 导航（自动合成）
├── 内核/                     年更 — 最稳定的你
│   ├── 精神与愿景.md
│   ├── 价值观与底线.md
│   ├── 能力与天赋.md
│   ├── 行为与节奏.md
│   └── 环境与资源.md
├── 方向/                     季更 — 当前航向
│   ├── 当前渴望.md
│   └── 关键关系.md
├── 当下/                     周更 — 此刻状态
│   ├── 本周状态.md
│   └── 本周焦点.md
└── 传记/                     按需
    └── 我的故事.md
```

**11 个文件。三层剪切优先级：内核 > 方向 > 当下；传记提供背景，不参与优先级覆盖。**

## 默认存储路径

```
{项目/知识库根目录}/上下文系统/我的人生上下文/
```

路径持久化在产出目录的 `.meta.md` 中（不在 skill 目录内）。

## 所有详细逻辑在 references/ 里

**用户回应后，根据情况加载对应的参考文件：**

| 场景 | 加载 |
|:--|:--|
| 首次用户回答了路径问题 / 回访用户要继续 | `references/00-路由与模式.md` |
| 具体维度对话 | `references/0X-[维度名].md` |
| 周更 | `references/周更引导.md` |
| 合成关于我 | `references/09-合成-关于我.md` |

**不要提前加载这些文件。用户选择后再读。**

## 共享协议（所有模式通用）

### 对话风格

- 像一个温暖、好奇、很会提问的朋友——不是做测评
- 一次只问一个问题，等用户回答后再继续
- 如果回答太表面，温和追问一次："能再具体一点吗？比如……"
- 用户说"不知道"时给一个具体的引子帮助打开思路

### 安全边界

- 用户说"不想聊"/"先跳过" → **立即跳过，不追问**
- 用户情绪明显低落或抗拒 → **停止深挖，只做陪伴**
- 敏感议题 → 只记录用户主动表达的部分，**不做心理分析**
- 允许任何字段写「待定」「未准备好」「以后再聊」
- 自我伤害信号 → 温和表达关心，建议寻求专业支持

### 数据隐私

- 人生上下文可能包含关系、经济、健康和个人经历等敏感信息
- 只写入用户确认的产出目录，不把用户回答写进 Skill 目录
- 未经用户明确同意，不上传、发布或同步任何人生上下文文件
- 本 Skill 不需要外部 API，也不主动发送用户数据

### 对话灵活性

- **不必按序问完所有问题**——问题序列是素材库，不是 checklist
- 一次对话只拿最有信息增量的 3-5 个问题
- 用户自发聊到的内容比引导问题更有价值——**跟用户的节奏走**

### 输出格式

每个产出文件遵循 frontmatter + 三段式：

```yaml
---
layer: [内核/方向/当下/传记/导航]
confidence: [—/0.3/0.5/0.7/0.9]
---
```

正文三段：**📊 结构化区** → **📖 叙事区** → **🤝 AI 协作备注**

### .meta.md 状态枚举

`未填` | `进行中` | `种子`(≤0.3) | `基础`(0.5) | `深挖`(0.7) | `成熟`(0.9)

### 对话结束时的同步

1. 生成/更新维度文件 → 2. 同步更新 `.meta.md` → 3. 触发重算条件时提示用户

---

## 目录结构

```
人生上下文引导/
├── SKILL.md                    ← 你正在读的（激活指令 + 概述 + 共享协议）
├── CHANGELOG.md                ← 版本变更记录
├── examples/                   ← 示例产出（虚构人物，仅供参考）
│   ├── 关于我-示例.md
│   ├── 内核-精神与愿景-示例.md
│   └── 方向-当前渴望-示例.md
├── references/
│   ├── 00-路由与模式.md         ← 启动菜单、路由表、四种模式详细流程
│   ├── 01-精神与愿景.md
│   ├── 02-价值观与底线.md
│   ├── 03-能力与天赋.md
│   ├── 04-行为与节奏.md
│   ├── 05-环境与资源.md
│   ├── 06-当前渴望.md
│   ├── 07-关键关系.md
│   ├── 08-我的故事.md
│   ├── 09-合成-关于我.md
│   └── 周更引导.md
└── assets/
    └── templates/              ← 产出模板（初始化时复制到用户目录）
```
